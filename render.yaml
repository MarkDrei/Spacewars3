services:
  - type: web
    name: spacewars-backend
    env: node
    buildCommand: npm install --production=false && cd packages/server && npm run build
    startCommand: cd packages/server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
