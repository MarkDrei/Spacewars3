This directory contains auto-generated screenshots of the application.

They are regenerated automatically by the GitHub Actions workflow on every push to `master`.
To regenerate locally, run:

```
npm run dev &         # start the app first
npm run screenshots   # then generate screenshots
```

Screenshots are committed back to the repo so they are always available in the README and other docs.
