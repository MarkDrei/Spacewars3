/**
 * Shared constants used across the server
 */

// Default starting position for newly created ships
export const DEFAULT_SHIP_START_X = 2500;
export const DEFAULT_SHIP_START_Y = 2500;
export const DEFAULT_SHIP_START_SPEED = 0;
export const DEFAULT_SHIP_START_ANGLE = 0;

// Default world size
export const DEFAULT_WORLD_WIDTH = 5000;
export const DEFAULT_WORLD_HEIGHT = 5000;
